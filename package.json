{
  "name": "venture-hack-root",
  "private": true,
  "dependencies": {
    "next": "16.1.6"
  },
  "scripts": {
    "build": "cd kisan-next && npm install && npm run build",
    "install": "cd kisan-next && npm install"
  }
}
